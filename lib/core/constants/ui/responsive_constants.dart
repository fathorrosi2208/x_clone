/// Defines breakpoints for responsive design
class Breakpoints {
  /// Minimum width for mobile devices
  static const mobile = 320.0;

  /// Minimum width for tablet devices
  static const tablet = 600.0;

  /// Minimum width for desktop devices
  static const desktop = 1024.0;

  /// Maximum content width for large screens
  static const maxContentWidth = 1200.0;
}

/// Responsive layout constants for consistent UI scaling
class ResponsiveLayoutConstants {
  // Default design sizes (based on iPhone 12 Pro)
  /// Base width for responsive calculations
  static const defaultWidth = 390.0;

  /// Base height for responsive calculations
  static const defaultHeight = 844.0;

  // Font sizes with responsive scaling
  static const fontSizeXS = 12.0;
  static const fontSizeS = 14.0;
  static const fontSizeM = 16.0;
  static const fontSizeL = 18.0;
  static const fontSizeXL = 20.0;
  static const fontSizeXXL = 24.0;

  // Consistent spacing scale
  static const spaceXXS = 4.0;
  static const spaceXS = 8.0;
  static const spaceS = 12.0;
  static const spaceM = 16.0;
  static const spaceL = 24.0;
  static const spaceXL = 32.0;
  static const spaceXXL = 48.0;

  // Standard component dimensions
  static const buttonHeight = 48.0;
  static const inputHeight = 56.0;
  static const cardPadding = 16.0;
  static const cardRadius = 12.0;
  static const iconSize = 24.0;
}
